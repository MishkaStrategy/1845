import fs from 'node:fs';const html=fs.readFileSync(new URL('./index.html',import.meta.url),'utf8');const css=fs.readFileSync(new URL('./styles.css',import.meta.url),'utf8');const js=fs.readFileSync(new URL('./script.js',import.meta.url),'utf8');const failures=[];if(!html.includes('hero-reference.webp'))failures.push('reference hero asset missing');if((html.match(/data-flicker/g)||[]).length!==5)failures.push('expected five flicker zones');if(!html.includes('hs-main-cta'))failures.push('hero CTA hotspot missing');if(!css.includes('aspect-ratio:3/2'))failures.push('reference artboard aspect ratio missing');if(!js.includes('Math.random'))failures.push('random Nixie flicker missing');if(failures.length){console.error(failures.join('\n'));process.exit(1)}console.log('Pixel-match QA OK');